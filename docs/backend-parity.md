# Backend Parity Audit

## 用途

`tools/api_parity_scan.py` 是一个轻量级接口盘点工具，用来给 Socartes-Rust 的后续全量后端替代提供现状对照。

它会同时扫描三侧：

- Rust 替代后端当前显式注册的 `axum` 路由
- DeepTutor Python 原后端的 FastAPI router 路径
- DeepTutor web 前端里常见的 `apiUrl(...)`、`wsUrl(...)`、`fetch(apiUrl(...))` 调用

输出是单个 JSON，适合做这些事情：

- 快速看 Rust 当前覆盖了哪些接口
- 找前端正在调用、但 Rust 还没接住的路径
- 找 Python 原后端存在、但 Rust 还未替代的路径
- 给后续更细的接口语义比对提供起点

## 当前替代目标

当前目标不是精确生成 OpenAPI，也不是验证请求体/响应体完全一致，而是先回答下面几个问题：

- Rust 后端当前暴露了哪些 HTTP 路径
- Python 原后端有哪些 router 路径仍然是基线能力
- 前端实际还在依赖哪些 API / WS 路径
- 三者交集和缺口大致在哪

这更适合做迁移前的 coverage 审计，而不是做最终兼容性证明。

## 2026-05-30 CLI 与 Streaming 增量

本轮对 Python `socartes_cli` / legacy `deeptutor_cli` 做了只读对照，当前 Python 顶层命令面为
`run/start/serve/chat/book/bot/kb/memory/plugin/config/session/notebook/provider`。
Rust CLI 已在单一实现文件 `backend/src/bin/socartes.rs` 中覆盖这些命令组，
并额外保留 Rust 初始化用的 `init wizard`。兼容入口现在包括
`socartes`、`socartes-cli`、`socartes_cli`、`deeptutor`、`deeptutor-cli`、
`deeptutor_cli`，避免 Python module-style 下划线命名在 Rust 包中缺可执行入口。

`/api/v1/ws` selected OpenAI-compatible LLM 的无工具首轮响应现在发送
`stream: true` 和 `stream_options.include_usage: true`，可以解析 chat-completions
SSE chunk：可见 `delta.content` 会按 chunk 逐条发 `content` 事件，完整答案仍合并后持久化；
`delta.reasoning_content`/`delta.reasoning` 不进入可见答案，只进入 assistant metadata。
带 native tool call 的首轮请求仍保持非流式，等 tool-call delta 聚合和 RAG 工具链事件完全补齐后再打开。

## 2026-05-30 TutorBot MCP Runtime 增量

TutorBot 的 MCP 支持从“配置可保存、工具名可放行”推进到 streamable HTTP
运行层闭环：bot `tools.mcp_servers` 现在会按 Python 的默认 `enabled_tools=["*"]`
语义解析，支持 `enabled_tools` 里的 raw MCP tool name 或已包装的
`mcp_<server>_<tool>` name；当 WebSocket chat turn 请求启用对应 `mcp_*`
工具时，Rust 会对 streamable HTTP MCP server 发 `initialize` 和 `tools/list`，
把 MCP `inputSchema` 原样暴露成 OpenAI-compatible function schema，并在
provider 返回 tool call 后把 `mcp_<server>_<tool>` 映射回原始 MCP tool name，
通过 `tools/call` 执行，再把 text content blocks 以 `\n` 合并成
`role:"tool"` message 回灌给第二轮 provider completion。

当前这仍是 MCP parity 的一个中间状态，不是完整替代：stdio 与 SSE transport
还没有真实 client runtime，streamable HTTP 也还没有覆盖完整 session/header
协商、取消语义、JSON schema cast/validation、超长 tool history 截断，以及
Python registry 的参数错误 retry hint 细节。

## 如何运行

在 `/home/coobabm/Socartes-Rust` 下运行：

```bash
python3 tools/api_parity_scan.py \
  --rust-root /home/coobabm/Socartes-Rust \
  --deeptutor-root /home/coobabm/.gitnexus/repos/DeepTutor
```

如果只想把结果保存下来继续分析：

```bash
python3 tools/api_parity_scan.py \
  --rust-root /home/coobabm/Socartes-Rust \
  --deeptutor-root /home/coobabm/.gitnexus/repos/DeepTutor \
  > /tmp/socartes-api-parity.json
```

## 输出内容

输出 JSON 主要包含四部分：

- `rust.routes`: 从 `backend/src/lib.rs` 提取的 Rust 路由
- `python.routes`: 从 `deeptutor/api/main.py` 的 `include_router(...)` 前缀和各 router decorator 合成出的 Python 路径
- `frontend.calls`: 从 `web/app`、`web/components`、`web/lib` 中提取的前端调用路径
- `summary`: 基于路径集合生成的计数、交集和缺口摘要

其中 `summary.parity` 会重点列出：

- `rust_vs_python_missing_in_rust`
- `rust_vs_frontend_missing_in_rust`
- `shared_http_paths_all_three`
- WebSocket 相关缺口

## 2026-05-29 当前快照

本快照来自：

```bash
python3 tools/api_parity_scan.py \
  --rust-root /home/coobabm/Socartes-Rust \
  --deeptutor-root /home/coobabm/.gitnexus/repos/DeepTutor \
  > /tmp/socartes-api-parity.json
```

当前计数：

- 前端扫描到的调用：`163`
- Python 原后端路由：`167`
- Rust 后端路由：`240`
- Python 原后端仍缺 Rust 覆盖的路径：`0`
- 前端仍缺 Rust 覆盖的路径：`1`，为 `/api/v1/book{param}` 扫描伪影

本轮已补齐并验证的主要区域：

- `/api/v1/knowledge/*` 课程/知识库启动、创建、上传、设默认、reindex、任务流、progress WebSocket、删除、health、configs/config sync、default get、per-KB config、progress、linked-folder/sync-folder 管理入口；`/api/v1/knowledge/list` 现在对齐 Python/live 后端返回 `KnowledgeBaseInfo[]` 数组；`/api/v1/knowledge/{name}/progress/ws` 会发送 Python/前端兼容的 `{"type":"progress","data":...}` 事件，create 会先写入带 `task_id` 的 `stage=initializing` progress，reindex 会先写入带 `task_id` 的 `stage=starting` queueing progress，再由 spawned worker 完成索引并写最终 progress，前端 `useKnowledgeProgress` 不再遇到课程 progress WS 404；`/api/v1/knowledge/tasks/{task_id}/stream` 现在 replay Rust 已登记 task 的 SSE backlog，未知 task 按 Python `ensure_task` 语义返回空 `text/event-stream` 流，不再伪造任意 task 的 completed 事件
- 课程 RAG index-version 状态：本地课程 reindex 会创建 Python-like `version-N/meta.json`，返回 `signature`/`noop`，并在 list/detail statistics 暴露 `index_versions`、`active_signature`、`active_match`、`rag_initialized`、`needs_reindex`
- 课程 RAG 持久化索引和向量检索：本地课程 reindex 现在会读取已保存的 embedding catalog，为 chunk 批量生成 embedding，并写入 `version-N/chunks.json` 与 `default__vector_store.json.embedding_dict`；检索优先读取 active signature 的 chunk vectors，对 query 使用同一 embedding 配置生成向量后按 cosine similarity 排序，source 输出保留旧 `source_id/title/content/confidence` 并补充 Python/LlamaIndex 风格 `provider/source/chunk_id/page/score`
- 课程资料解析：`/api/v1/knowledge/supported-file-types` 现在对齐 Python `FileTypeRouter` 的 parser + text-like 扩展白名单，包含 `.pdf/.docx/.xlsx/.pptx` 和常见文本/代码格式；索引与未索引 fallback 检索共用同一抽取入口，文本文件使用 UTF-8/BOM、GBK、Windows-1252 fallback，PDF 保留 `--- Page N ---` marker，DOCX/XLSX/PPTX 通过 OOXML ZIP/XML 抽取正文、sheet 和 slide 文本；坏 parser 文件在 KB loader 语义下跳过，不会把二进制垃圾写入 chunk index
- 课程原文件预览：`GET /api/v1/knowledge/{name}/files/{path}` 现在按 Python `FileResponse(..., content_disposition_type="inline")` 语义服务 KB 原文件，PDF/PNG 等常见预览文件返回正确 `Content-Type` 和 `inline` `Content-Disposition`，并按 raw/files 目录沙箱保留子目录路径而不是压成 basename；路径逃逸返回 `403 Access denied`
- 课程创建后台初始化：`/api/v1/knowledge/create` 现在按 Python `run_initialization_task` 语义同步校验/落盘后立即返回 `kb_init_...` task id，先写 `stage=initializing` progress，再由 spawned worker 调用 active embedding provider 创建 `version-N/chunks.json` 和 vector store，更新 metadata/index_versions/active_match，并通过 task SSE replay `process_log/progress/complete`；慢 embedding/provider 不会阻塞 create HTTP 响应
- 课程上传增量索引：`/api/v1/knowledge/{name}/upload` 现在要求课程已有 active index；当课程配置或 metadata 标记 `needs_reindex`、或课程尚未 reindex 出 active index 时返回 `409`，避免在 stale/missing index 上假成功；完成 reindex 后上传会先保存文件并立即返回 `kb_upload_...` task id，再由 spawned worker 重写 active `chunks.json`、维护 `metadata.json.file_hashes`、追加 `update_history`，慢 embedding/provider 不会阻塞 HTTP 响应；已存在课程内容的重复上传仍不改 metadata；坏 DOCX/PDF/XLSX/PPTX parser 文件按 Python `DocumentAdder.process_new_documents` 语义逐文件跳过，后台 task 仍 `completed`，`indexed_count=0`、`index_changed=false`，且不把坏文件写入 `file_hashes` 或 chunk source
- 课程 task id/SSE 形状：create/upload/reindex/sync-folder 现在使用 Python 风格 `kb_init_YYYYMMDD_HHMMSS_xxxxxxxx`、`kb_upload_...`、`kb_reindex_...` task id；`/api/v1/knowledge/tasks/{task_id}/stream` 使用 Python 风格共享 task backlog 和 live subscriber stream，输出 Python 兼容 `process_log`、`progress`、`complete` / `failed` 事件，`process_log` 含 `type/level/message/logger/timestamp/context.task_id/capability/sink`，`complete` payload 为 `{detail, task_id}`，`failed` payload 为 `{detail, task_id, details?}`，未知 task 不再返回 JSON 404，而是按 Python `ensure_task` 语义创建空 `text/event-stream` 流；create/upload/reindex/sync-folder 现在在入队后立即返回 `200 + task_id`，慢 embedding/provider 不会阻塞 HTTP 响应，索引执行阶段失败会写入 error progress + `failed` SSE，而不是同步返回 400；reindex 成功路径会在 `Queueing re-index...` 之后 replay `stage=processing_documents` 和 `Embedding batches: 1/1` 进度，再用 `process_log + complete` 终止，不额外发送 Python 没有的 `progress stage=completed` frame；reindex 失败现在会 replay 带上下文的 `Re-index failed: ...` error log、单独 `Stack trace:\n...` process log、`progress.stage=error`，并把原始底层错误放在 `failed.detail`、traceback 诊断放在 `failed.details`；sync-folder 缺 active index 的后台失败现在会按 Python upload worker 语义写 `Processing failed: Upload processing failed...` progress message、`failed.detail` 外层包装错误和 `failed.details` traceback 诊断
- 课程 linked-folder sync 状态：`sync-folder` 现在递归检测 linked folder 中的 supported files，按 `synced_files` 的修改时间区分 `new_files` / `modified_files`；`needs_reindex` KB 会在启动后台 task 前按 Python `_assert_kb_writable_or_409` 语义返回 `409`，不创建 `kb_upload_*` task；未标记 `needs_reindex` 但缺 active LlamaIndex index 的 KB 会先返回 `kb_upload_...` task id，再在后台失败，且不会复制 linked 文件、不会更新 `last_sync/synced_files`、不会写 `last_indexed_action`；有变更且 KB 可写时先返回 `kb_upload_...` task id，再由 spawned worker 复制文件、按 upload 语义更新 active chunk index/metadata/RAG source，并只按实际成功抽取出 chunk 的文件写回 `synced_files` 和 `file_count`；坏 parser 文件会被复制到课程 raw files 但不计为 processed、不写入 `synced_files`；未变化时返回 `No new or modified files to sync` 且不创建 task id，避免每次 sync 都重复全量处理
- 课程 RAG 检索边界：选定上传课程时只从该课程返回匹配 source；无匹配时返回空 sources，不再回退到内置 `socartes-rust-rag` 资料造成来源串库
- `GET /api/v1/knowledge/{kb_name}` KB 详情入口已补齐，返回 Python 兼容的 `id/source/assigned/read_only/path/metadata/statistics/status` 字段；缺失课程返回 `404 detail="Knowledge base '<name>' not found"`
- `/api/v1/course*` 与 `/api/v1/courses*` legacy course aliases 已接到现有 `/api/v1/knowledge*` 行为：`/api/v1/courses`、`/api/v1/courses/`、`/api/v1/course` 和 `/api/v1/course/` 的 `GET` 都与 `/api/v1/knowledge/list` 返回同形 `KnowledgeBaseInfo[]` 数组，`POST /api/v1/course(s)` 复用课程创建，默认课程仍走 `/api/v1/course(s)/default`，`/api/v1/course(s)/{name}` 返回课程详情，并覆盖 configs/default/files/progress/upload/reindex/linked-folder 等课程管理入口，避免旧前端或外部调用方在 course 命名路径上拿到 404/405 或不一致 payload
- Python legacy 课程目录布局已原生兼容：当 `SOCARTES_KNOWLEDGE_ROOT` 直接指向原 `data/knowledge_bases` 时，Rust 会同时识别新布局 `bases/{kb}/files` 和旧布局 `{kb}/raw`，读取 `{kb}/metadata.json`、`{kb}/version-*`、旧 `.progress.json`，并从 `kb_config.json.defaults.default_kb` 恢复默认课程；线上 `sc.tckr.top` Rust trial 现在直接使用 `/home/coobabm/DeepTutor/data/knowledge_bases`，不再依赖课程文件 overlay
- `/api/v1/auth/*` standard JSON auth core：auth disabled 仍返回 Python 的 local-admin/no-login-required 语义；auth enabled 现在支持首用户公开注册并自动升 admin、bcrypt 密码 hash/verify、HS256 JWT、`dt_token` cookie、Bearer/cookie status、错误密码 401、admin 创建/列用户且不泄露 hash、普通 user 访问 admin list 403、`DELETE /auth/users/{username}`、`PUT /auth/users/{username}/role`、不能删/降权自己和 missing user 404；Rust 现在会迁移 Python legacy `data/user/auth_secret` 到 `multi-user/_system/auth/auth_secret`，迁移 `data/user/auth_users.json` 的 flat string / `hash` / `password_hash` 旧格式到 canonical users，按 Python 规则给首个有效 legacy 用户默认 `admin`、后续默认 `user`，并支持 `AUTH_USERNAME` + `AUTH_PASSWORD_HASH` 的只读 env-admin fallback 且不把 env 用户落盘；auth-enabled 时业务 HTTP 路由现在通过统一 middleware 对齐 Python `_auth = [Depends(require_auth)]`，未登录返回 `401 {"detail":"Not authenticated"}` + `WWW-Authenticate: Bearer`，无效 token 返回 `401 {"detail":"Invalid or expired token"}`，同时 `/health`、docs、OpenAPI、auth bootstrap 和 `/api/outputs/*` 保持公开
- `/api/v1/auth/*` PocketBase auth mode：当 `AUTH_ENABLED=true` 且配置 `POCKETBASE_URL` 时，Rust 现在走 PocketBase REST auth 分支；`POST /auth/login` 调 `/api/collections/users/auth-with-password`，把 raw PB token 写入 `dt_token` cookie，`GET /auth/status` 和 protected HTTP middleware 通过 `/api/collections/users/auth-refresh` 验证 Bearer/cookie token 并缓存 60 秒；`POST /auth/register` 和 admin `POST /auth/users` 会创建 PB `users` record，返回 Python 风格 non-admin user body，不再误写本地 users.json 或把 PB first user 升成本地 admin；PB auth 模式下 `POST /api/v1/internal/test-chat-turn` 现在把完成 turn 拆写到 PocketBase `sessions/messages/turns/turn_events` 四个 collection，terminal persist 不再先把 session 标为 completed，而是先确保 running shell，写完 messages/turn/events 后才更新 final status；如果 `messages.metadata_json` 等 PB schema 拒绝导致持久化失败，会返回 `502` 并清理本次 turn/session shell，避免留下 completed session 或半条 message；`GET /api/v1/sessions` 和 `GET /api/v1/sessions/{id}` 从 PB records 聚合出前端兼容的 summary/detail（含 messages、active_turns、turns、turn_events），`PATCH /api/v1/sessions/{id}` 会 PATCH PB session title，`DELETE /api/v1/sessions/{id}` 会级联删除 PB session、messages、turns、turn_events，且该路径不再生成本地 `data/sessions/*.json`；PB raw token 现在可直接用于 unified `/api/v1/ws?token=...`，running turn 的 partial events 会 upsert 到 PB `sessions/turns/turn_events`，`subscribe_turn`/`subscribe_session` 可从 PB replay 后继续接 live events，terminal persist 会更新同一 session/turn 并按 `(turn_id, seq)` 避免重复 event records；PB-backed stored running turn cancel 现在会在 PB `turn_events` 追加 `error(status=cancelled, turn_terminal=true)` 与 `done(status=cancelled)`，把 PB turn/session 标为 `cancelled`，replay 可读到 terminal events，且不写本地 session JSON、不改 messages；PB-backed `regenerate` 现在从 PB session/messages 聚合读取上一条 user 和尾部 assistant，只删除尾部 assistant message record，不删除/标记旧 turn 或 turn_events，复用原 user message、不重复创建 PB session/本地 session JSON，并在新 turn session event 暴露 PB 字符串 record id 形态的 `regenerated_from_message_id` 与 `superseded_turn_id`；PB-backed `quiz-results` 现在对 PB-only session 写入 `messages` collection 的 `deep_question` quiz message，仍按 Python 语义 upsert question notebook，且不生成本地 session JSON
- `/api/v1/multi-user/*` 资源授权入口：Rust 现在补齐 Python router 的 `GET /me/access`、`GET /admin/resources`、`GET /users`、`GET|PUT /users/{user_id}/grants` 和 `POST /users/{user_id}/spaces/assign`；普通用户只能看到 redacted model access、自己的/已分配的课程、分配 skill id 与 spaces，admin resources 只返回 profile/model/resource summary 且不泄露 `api_key/base_url/token/path` 等运行时 secret；grant 保存会强制 `user_id` 使用 path 参数、补齐 `models.llm|embedding|search`、`knowledge_bases`、`skills`、`spaces` 默认结构，并按 Python 规则拒绝任意深度的 secret/path 字段；空间分配会从 admin workspace 复制模板到 `multi-user/{uid}/user/workspace`，写 `.socartes_provenance.json` 并追加 `grant.spaces`
- 多用户课程/技能隔离：auth-enabled 普通用户的 `/api/v1/knowledge/list` 和 detail 现在只解析自己的 `multi-user/{uid}/knowledge_bases/*` 与 grant 分配的 admin KB；assigned admin KB 返回 `source=admin/assigned=true/read_only=true` 且隐藏 `path`，未分配 admin KB 裸名返回 404、显式 `admin:kb:*` 返回 403，assigned admin KB 的 upload/reindex/delete 返回 `403 "Assigned admin knowledge bases are read-only"`；普通用户直接调用 plugin RAG 未选择 KB 时返回 `400 "No knowledge base selected..."`，不再回退到内置/admin 课程；unified `/api/v1/ws`、legacy `/api/v1/chat`、legacy `/api/v1/solve`、Playground capability stream 和 internal test chat turn 现在都会把 `AuthTokenPayload` 传入 chat turn/RAG/provider tool-call 链路，普通用户在 chat/RAG 中请求未分配 admin KB 会得到 auth-scoped retrieval failure，不会泄露 admin KB 内容，legacy solve 普通用户缺 `kb_name` 时也不会绕回内置/admin 课程。`/api/v1/skills/list` 和 detail 现在按 user workspace + assigned admin skill 合并，assigned skill `source=admin/read_only=true`，未分配 skill 403；create/update/delete 对普通用户只写 `multi-user/{uid}/user/workspace/skills`，同名 user skill 会 shadow assigned admin skill
- `/api/v1/sessions/*` 和 `/api/v1/chat/sessions/*` 会话列表、详情、改名、删除、quiz results；`POST /api/v1/sessions/{session_id}/quiz-results` 现在按 Python 语义拒绝空 answers、写入 `capability=deep_question` 的 `[Quiz Performance]` 用户消息，并 upsert question notebook entries
- `DELETE /api/v1/chat/sessions/{session_id}` 和 `DELETE /api/v1/solve/sessions/{session_id}` legacy alias 已补齐，返回 Python 兼容的 `{"status":"deleted","session_id":...}`，缺失 session 返回 `404 detail="Session not found"`；新版 `/api/v1/sessions/{session_id}` 仍保留原 `{"deleted":true}` 合同
- Python 旧协议 `WS /api/v1/chat` 已新增 compatibility adapter：接受旧首帧/多轮 payload `message/session_id/history/kb_name/enable_rag/enable_web_search/language`，内部复用 Rust turn 构建与持久化，向外映射为 Python 事件 `session`、`status(stage=rag|web|generating)`、`stream`、`sources(rag/web)`、`result`、`error(message)`；`enable_web_search=true` 现在会按 Python 旧路由先发送 `Searching the web...` 状态，把 `web_search` 接入 turn tools，并在最终 sources 事件中返回非空 `web` citations；空 `message` 发 `{"type":"error","message":"Message is required"}` 并保持连接
- Python 旧协议 `WS /api/v1/solve` 已新增 compatibility adapter：接受旧 payload `question/tools/kb_name/session_id/detailed_answer`，不传 `tools` 时按 Python `DeepSolveCapability.manifest.tools_used` 默认启用 `rag/web_search/code_execution/reason`，且 RAG 默认课程为 `ai-textbook`；发送 Python 形状 `session -> task_id -> status(started) -> agent_status/token_stats/progress -> result|error`，成功或错误后主动关闭连接；`task_id` 现在使用 Python `TaskIDManager` 风格的 `solve_YYYYMMDD_HHMMSS_xxxxxxxx`，选中真实 OpenAI-compatible provider 时会把 provider `usage` 映射为 Python `DisplayManager.stats` 形状的 `model/calls/tokens/input_tokens/output_tokens/cost`；成功 result 现在会创建 `data/user/workspace/chat/deep_solve/solve_YYYYMMDD_HHMMSS` 输出目录、写入 raw `final_answer.md`、Python 形状 `scratchpad.json` 和 provider usage 驱动的 `cost_report.json`，返回非空 `output_dir/output_dir_name` 和 Python 风格 `metadata.mode/timestamp/output_dir/total_steps/completed_steps/plan_revisions/cost_summary`，并把 result 中的 `](artifacts/...)` 链接改写为 `/api/outputs/workspace/chat/deep_solve/<solve_dir>/artifacts/...`；当前仍是 wire-contract compatibility，内部 solver 还没有完整移植 Python `MainSolver` 的 planner/solver/writer 执行链和 process-log 细节
- `/api/v1/ws` unified chat WebSocket：`start_turn/message` 会持久化完整 turn event 序列和 Python/frontend 对齐的 `metadata.request_snapshot`，覆盖 attachments、notebook/history/question/book references、skills、memory references、config 与 `llmSelection`；`llm_selection` 会按 catalog 中同一 profile 下的 model id 精确校验，无效 selection 返回 `error(status=rejected)` 且不创建 turn；`subscribe_turn`/`resume_from` 可按 `seq` 回放已完成 turn 的尾部事件，也可在 turn 运行中接上 live events；`cancel_turn` 支持同连接、跨连接和已落盘 running turn 取消，并广播/补写 `error(status=cancelled)` + `done(status=cancelled)`；`regenerate` 复用上一条 user、删除尾部 assistant、写入 regenerate metadata 且不重复 user message，无 override 时会按 Python 优先级回放上一条 user snapshot 里的 `llmSelection` 和 `bookReferences`，并把选中书页序列化为 `[Book Context]` 注入 provider 当前 user message；无效 regenerate `llm_selection` 会在删改历史前 rejected，保留原 user/assistant 历史；`subscribe_session` 可回放 session 最新 turn
- `/api/v1/ws` selected LLM runtime：当 turn 选择非 `deterministic-agent-loop` 的 OpenAI-compatible profile/model 时，Rust 会从 settings catalog 解析 `base_url/api_key/api_version/extra_headers/model/reasoning_effort`；普通模型保持 `{base_url}/chat/completions` non-streaming 成功路径，发送 `system + bounded history + current user` messages 和 `stream:false`；`gpt-5*`/`o1`/`o3`/`o4` 或配置了非 `none` reasoning effort 的模型会走 `{base_url}/responses`，把 chat messages 转成 `instructions + input[]`，tools 转成 Responses flat function tools，发送 `store:false`、`stream:false`、`reasoning.effort` 和 `include:["reasoning.encrypted_content"]`，并把 Responses `output_text`、`reasoning.summary`、`input_tokens/output_tokens` 解析回现有 assistant content/metadata；chat completion 响应里的 `usage`、`finish_reason`、`message.reasoning_content`/`message.reasoning` 会作为 metadata 保存在 content event 与 assistant message 上，visible answer 不会混入 hidden reasoning；text-like/base64 文件附件会落盘到 `/api/attachments/{session_id}/{attachment_id}/{filename}`，提取 UTF-8 文本后以 `[Attached Documents]` + `[User Question]` 形式注入 selected LLM 的当前 user message，持久化 user message 保留原始问题并清空附件 `base64`；当 turn 启用工具且 provider 支持 native tool calling 时，会发送 OpenAI-compatible `tools` schema 与 `tool_choice:"auto"`，解析 assistant `tool_calls`，截断并归一化 tool call id，复用本地 `plugin_tool_result` 执行工具，把 selected course/RAG 的 `kb_name` 在后端强制绑定到当前 turn 选中的课程，RAG query 为空或 arguments 解析失败时回退到当前用户问题，再用一致的 assistant tool_calls + `role:"tool"` messages 发第二轮 completion 生成最终回答；第二轮会禁用 tools/tool_choice，避免重复 tool loop；provider HTTP/JSON/content 错误会发 terminal `error(status=failed)` + `done(status=failed)`，session/turn 标为 `failed`，不写入假的 assistant completed 回答
- OpenAI-compatible Responses fallback：`/api/v1/ws` selected LLM runtime 和 `/api/v1/system/test/llm` 共用同一 provider 调用层；当 GPT-5/reasoning 路径先请求 `{base_url}/responses`，而 provider 返回 Python 兼容的 `400/404/422` unsupported/unknown-parameter/Responses API 不支持错误时，Rust 会保留原始 chat body 并自动 fallback 到 `{base_url}/chat/completions`，成功后仍按 selected profile/model 写入 assistant metadata；`500/401` 等非契约错误仍保持 provider failure，不伪造完成回答
- `/api/v1/book/*` Book 首页、file-backed 书籍读取、创建、确认 proposal、确认 spine、编译页面、块编辑、deep dive、quiz attempt、supplement、page chat session、rebuild、health、fingerprint refresh、WebSocket 入口；`confirm_spine` 现在会按 Python BookEngine v2 语义注入 ready overview 页，并生成前端 `ConceptGraphBlock` 可直接渲染的 `text + concept_graph + text chapter index` 三块结构，`concept_graph.payload.code.content`、`payload.graph`、`payload.index.chapters` 均非空；非 overview 页不再停留在单个 `Socartes learning trace` Rust 占位块，`confirm_spine(auto_compile=true)` 与 REST/WS `compile_page` 会复用同一静态 Python-like planner，为 `theory/derivation/history/practice/concept` 生成 `section/figure/callout/code/quiz/flash_cards/timeline/interactive/animation` 等 ready blocks，并保留已有 block id 以免前端持有的 block 引用失效；ready page 在 `force=false` 时按 Python `_compile_page_claimed` 语义直接返回，不再通过 Book WS 发 `page_compile_started/page_compiled`、不写 page 文件、不追加 `compile_page` log；已有真实非 overview blocks 的 `force=true` 现在按 Python `_reset_page_for_force_compile` 语义保留原 block 列表、顺序、id、type、title、params 和 user_note，只刷新 generated block 的 payload、清空 error/source_anchors，并把 metadata 收敛到 `transition_in/deep_dive_page_id` 后重新生成；已有 ready overview blocks 的 `force=true` 现在按 Python overview 例外语义保留 block 列表和每个 block 的 payload/params/metadata/source_anchors/error/status，不再重新 materialize 成默认三块；overview 已有 blocks 但部分非 ready 时现在按 Python compiler 语义保留列表，ready block 完全跳过，非 ready `concept_graph` 等 block 按 generator 生成 payload/metadata 后再聚合 page status，缺少 `params.concept_graph` 时按 Python `GenerationFailure` 写入 block `error` 与 `metadata.failure`，并保留旧 payload/source_anchors；REST/WS `regenerate-block` 现在复用同一生成记录路径，合并 `params_override` 后按 Python 语义生成或失败，缺少 `concept_graph` 的图块不再被伪标为 ready，页面会重新聚合为 partial/error；当 settings catalog 选中真实 OpenAI-compatible LLM 时，非 overview、非 `concept_graph/user_note` 的 regenerate 会调用 selected provider，prompt 包含 book/chapter/page/source anchors，返回 `metadata.generator=selected_llm_provider`、provider/profile/model/usage 信息和 `metadata.regenerated=true`，无 provider 时保留静态 fallback；Book WS 找不到 regenerate 目标时按 Python 差异返回 `regenerate_block_result` 且 `block:null`，HTTP 仍保持 404；已有 block 状态聚合现在按 Python `BookCompiler._finalize_page_status` 写入具体 `page.error`，包含无 blocks 文案路径以及全部失败/部分失败文案；`deep-dive` 现在会追加 synthetic chapter、创建 child page、回写 parent page `links[]`，并把 `metadata.deep_dive_page_id` 写回触发 block，匹配前端 `DeepDiveBlock` 的重复触发保护；Book WS `create` 现在会返回 Python 形状的 `stage_start(ideation)`、`progress(metadata.kind=proposal_ready)`、`stage_end` 和 `create_result`，`confirm_proposal`/`confirm_spine`/`compile_page`/`regenerate_block` 会复用 file-backed Book helpers 并发出 Python 风格 `exploration_ready`、`spine_ready`、`overview_ready`、`page_compile_started`、`page_compiled`、`block_started`、`block_ready` progress 与对应 result payload；quiz attempt 进度按 Python 语义使用整数累计 `score` 并在答错时记录 `weak_chapters`
- Book supplement/remediation：`/api/v1/book/books/supplement` 现在按 Python `supplement_for_weakness` 语义连续追加 `callout -> text -> quiz` 三个 ready block；callout `params.variant=common_pitfall`，text `params.role=remediation`，quiz `params.num_questions=2/difficulty=easy`，响应返回最后追加的 quiz block 而不是旧的单个 callout
- Book health / KB drift：`GET /api/v1/book/books/{book_id}/health` 现在返回 Python `kb_drift` 字段形状，包含 `current_fingerprints`，会按课程文件 fingerprint 对比 `kb_fingerprints`，检测 changed/new/removed KB，drift 时把所有 ready page 写入 `stale_page_ids` 并持久化 manifest；`log_health` 现在解析 Python `log.md` 行格式，统计 `total_entries/error_entries/block_failures/last_compile_at/last_error_at/repeated_failures`，缺失日志时 timestamp 字段返回空字符串而不是 `null`
- Book fingerprint refresh：`POST /api/v1/book/books/{book_id}/refresh-fingerprints` 不再写入 `rust-file-*` 占位值，而是按 Python 等价算法读取课程原始文件名、mtime seconds、size 生成 SHA-256 fingerprint，清空 `stale_page_ids`，保存 manifest，并追加 `**kb_health**` log 行；fingerprint helper 同时支持 Rust `knowledge/bases/{kb}/files` 和 Python legacy `knowledge/{kb}/raw` / `knowledge/knowledge_bases/{kb}/raw` 迁移布局
- `/api/outputs/{path}` 兼容原 DeepTutor public output 静态文件白名单
- `/api/v1/settings*` UI/catalog/apply/themes/sidebar/test SSE/llm-options 兼容入口；auth-enabled 时 `GET /api/v1/settings` 现在按 Python 角色语义裁剪响应，普通 user 只拿 `ui + model_access` 且不暴露 `catalog/providers`，admin 拿 `ui/catalog/providers`；`GET|PUT /settings/catalog` 和 `POST /settings/apply` 已加 settings admin guard，普通 user 返回 `403 detail="Model configuration is managed by an administrator."`；catalog 响应按 Python 使用 `********` 遮罩非空 `profile.api_key`，提交 masked key 时恢复磁盘旧 secret，apply 和 tour complete 返回的 `catalog/env` 均脱敏；`GET /settings/llm-options` 不再返回硬编码 `deterministic-agent-loop`，admin 响应从 saved catalog 动态生成 `active + options[]`，普通 user 响应按 model grant 过滤为 `active:null` 与已分配模型列表且不泄露 `api_key/base_url/api_version/extra_headers`
- `/api/v1/system*` status/runtime-topology/test LLM/test embeddings/test search 兼容入口；`/api/v1/system/runtime-topology` 现在返回 Python operator contract 的 `TurnRuntimeManager/ChatOrchestrator/SQLiteSessionStore/CapabilityRegistry/ToolRegistry` 和 `chat/solve/question/research` compatibility routes；`/api/v1/system/status` 的 `search` 对象现在按 Python resolver 语义区分 `configured`、`fallback`、`unsupported`、`not_configured`，fallback 暴露 effective provider `duckduckgo` 和对应 error 文案，unsupported/missing credentials 暴露 Python 对齐错误文案，auth-enabled 普通 user 会按 Python 裁剪 `llm.model`、`embeddings.model`、`search.provider`；`/api/v1/system/test/embeddings` 现在按 Python 语义执行真实 batch probe，校验两条向量返回、非空、维度一致和数值向量元素，并区分配置错误、连接错误和 invalid response；OpenAI-compatible/Azure 会处理 `api-version`、`api-key` 和 `extra_headers`，Cohere v2/Ollama 会使用各自的 payload 与 response shape
- `/api/v1/settings/tests/embedding/start` + `/events` 现在按 Python settings runner 语义保存 catalog 快照、返回 `embedding-<10 hex>` run id，SSE 执行真实 embedding probe，probe 文案已从旧 `DeepTutor ...` 对齐为 `Socartes embedding smoke test` / `Socartes retrieval batch probe`，成功时发送 `capabilities`/`response`/`catalog`/`completed`，失败时发送 terminal `failed` 且不误报 completed；未知 diagnostic service 现在同 Python runner 一样发送 `failed: Unsupported service: ...`，不再走 deterministic compatibility completed；成功 probe 会从已知模型能力表刷新 `default_dim`、`supported_dimensions`、`supports_variable_dimensions`、`model_known` 并写回 catalog；`settings/tests/{service}/start|events|cancel` 现在需要 settings admin，普通 user 返回 `403 detail="Model configuration is managed by an administrator."`
- `/api/v1/settings/tests/llm/start` + `/events` 现在会保存前端提交的 catalog 快照、返回 `llm-<10 hex>` run id，提交 masked catalog 时先按 Python `_catalog_for_write` 语义恢复磁盘旧 secret，SSE 使用 active LLM profile/model 调用 OpenAI-compatible provider，普通模型走 `{base_url}/chat/completions`，GPT-5/reasoning 模型共用 `{base_url}/responses` 请求转换并把 Responses payload 转回 chat-compatible JSON，透传 `api_key/api_version/extra_headers`，发送 `info`/`response`/`context_window`/`catalog`/`completed`，并把检测到或默认的 `context_window`、`context_window_source`、`context_window_detected_at` 写回 `catalog.json`
- `/api/v1/system/test/llm` 现在按 Python 语义读取 settings catalog 的 active LLM profile/model，普通模型调用 OpenAI-compatible `{base_url}/chat/completions`，GPT-5/reasoning 模型调用 `{base_url}/responses` 并解析 `output_text`，成功时返回 `LLM connection successful`，空响应、配置错误和 provider 请求错误都会返回 `success:false`，不再给出假的 deterministic 成功
- `/api/v1/system/test/search` 和 `/api/v1/settings/tests/search/start|events` 现在按 Python search resolver 语义处理 active search profile：`openrouter/exa/baidu` 等 deprecated/unsupported provider 返回失败，`perplexity/serper` 缺 key 返回 provider-specific missing credentials，`brave/tavily/jina` 缺 key 和 `searxng` 缺 base_url 会 fallback 到 `duckduckgo`；`duckduckgo` 会真实调用 DuckDuckGo HTML search endpoint，测试环境可通过 profile `base_url` 指向本地 mock，按 Python `DDGS.text()` 行形状解析 `title/href/body` 到 `citations/search_results/sources`，`source` 固定为 `DuckDuckGo`，不再落到 deterministic stub；`searxng` 配置了 `base_url` 时会真实调用 `{base_url}/search?q=...&format=json`，解析 Python provider 的 `results[].title/url/content/engine` 到 `answer/search_results/sources`；`brave` 配置了 `api_key` 时会真实调用 Brave Search `GET {base_url_or_default}/res/v1/web/search?q=...&count=...`，透传 `Accept: application/json` 和 `X-Subscription-Token`，解析 Python provider 的 `web.results[].title/url/description/age` 到 `answer/citations/search_results/sources`；`tavily` 配置了 `api_key` 时会真实调用 `POST {base_url_or_default}/search`，发送 Python 默认 body `api_key/query/search_depth=basic/topic=general/max_results/include_answer=true/include_raw_content=false/include_images=false`，并按 `supports_answer=true` 直接使用 provider `answer`、解析 `results[].title/url/content/published_date/source/raw_content/score` 和 `images/response_time`；`perplexity` 配置了 `api_key` 时会真实调用 OpenAI-compatible `POST {base_url_or_default}/chat/completions`，发送 Python SDK 等价的 `model=sonar` 与 system/user messages，解析 `choices[0].message.content`、`usage/cost`、`citations` 和 `search_results`；`jina` 配置了 `api_key` 时会真实调用 `GET {base_url_or_default}/{urllib.quote(query)}`，透传 Bearer auth 和 enrich headers，并把 enriched `images/publishedTime/metadata/external` 放到 sources attributes；`serper` 配置了 `api_key` 时会真实调用 `POST {base_url_or_default}/search`，透传 `X-API-KEY` 和 Python 默认 `q/num/gl/hl/page/autocorrect` body，解析 `organic[]`、`answerBox`、`knowledgeGraph`、`peopleAlsoAsk`、`relatedSearches` 到 sources 和 provider metadata，不再落到 deterministic stub；settings search SSE 支持空 body start，并发送 `info/config/warning/response/completed` 或 terminal `failed`
- `/api/v1/tutorbot*` TutorBot 列表、创建/启动、停止/销毁、详情/PATCH、recent、souls CRUD、channels schema、profile 文件、history、WebSocket 聊天入口；TutorBot WS 现在会读取 bot `llm_selection`/消息 override，调用选中的 OpenAI-compatible provider，发送 persona/workspace 文件/history/user message，支持 native `tools` + `tool_choice:"auto"`，复用本地 RAG/tool adapter 执行 provider `tool_calls`，再用 assistant `tool_calls` + `role:"tool"` messages 二次 completion 生成最终回答；deterministic selection 保留旧兼容 fallback；Web chat 历史按 Python `bot:{bot_id}` canonical session key 写入 `bot_<bot_id>.jsonl`，不同 `chat_id` 不再分裂上下文；session JSONL 现在以 Python `_type=metadata/key/created_at/updated_at/metadata/last_consolidated` header 开头，history reader 会跳过 metadata；`/help` slash command 会返回 Python AgentLoop 长版命令清单且不写历史、不调用 provider；`/new` 会先把当前 canonical session 的 user/assistant 消息以 Python raw archive 形状追加到共享 `SUMMARY.md`，归档成功后才重写 canonical session 为 metadata-only header 并返回 `New session started.`；`/stop` idle 路径返回 `No active task to stop.` 且不写历史、不调用 provider；`/restart` 返回 Python 固定文案 `Restarting...`，不调用 provider、不写 `/restart` 到 history，也不清空已有 canonical history；空 `/btw` 与精确 `/team` 返回 Python usage 文案且不写历史/不调用 provider；带参数 `/btw <instruction>` 现在按 Python 形状立即返回 `BTW accepted (id: <8 hex>). I'll send the result when it finishes.`，不进入普通聊天 loop、不写普通 history，并在后台使用当前 selected OpenAI-compatible provider 执行单 subagent 任务，完成后通过当前 TutorBot WebSocket notification 分支发送 `type=proactive` 结果；空闲 `/team status` 返回 `No active nano team...` 并只写 metadata header 的 `nano_team_active=false`，空闲 `/team stop` 返回 `No active team.` 并移除该 metadata key，不写普通 history、不调用 provider
- TutorBot nano team 基础 runtime：`/team <goal>` 与 `/teams <goal>` 现在会在普通聊天 loop 前短路，发送 Python web 形状的 lead update `metadata.team_event=true` 和 ack `metadata.team_text=true`，创建 `workspace/teams/bot_<bot_id>/<run_id>/config.json/tasks.json/NOTES.md/events.jsonl/workers/`；bot 配置了 `llm_selection` 时，启动 team 会先调用当前 selected OpenAI-compatible provider 生成初始 `mission/members/tasks/notes` JSON plan，支持从 assistant 文本或 markdown fence 中抽取 JSON，最多两次尝试，第一次 malformed JSON、duplicate task ids、cycle 等 validation error 会按 Python repair prompt 追加 `Previous output error: ... Fix and output valid JSON only.`；normalization 会清空不存在的 owner 为 `null`、过滤未知 dependencies、拒绝重复 task id 和依赖环，按 provider 成员动态创建 worker，并在 `config.planning` 与 `team_started.data.plan_source` 记录 `provider`；两次 provider 输出仍无效或 deterministic selection 时回退 Python fallback 形状：`researcher/builder` 两个成员、`Analyze the request` / `Execute and report` 两个任务、`# Team Notes\n- Keep changes minimal and reliable.\n` notes；`/team status` 读取 active runtime 渲染成员、任务摘要和 approval queue，`/team log [n]` 读取 JSONL 事件并支持默认/截断 limit，`/team stop` 把未完成 runtime 置为 `paused`、追加 `team_stopped`、移除 `nano_team_active`；`/team approve <id>`、`/team reject <id> <reason>`、`/team manual <id> <instruction>` 现在按 Python board 语义更新 `tasks.json`，追加 `task_approved/task_rejected/task_change_requested` 事件，manual 会写入 `mailbox.jsonl` 给任务 owner；当 active team 存在 `awaiting_approval` 任务时，普通 Web 消息会在聊天 loop 前按 Python `handle_approval_reply` 语义处理自然语言 approve/reject/change 回复，包括单 pending task id 推断、多 pending 要求显式 task id、反馈文本清理、`team_text` metadata 和不写普通 history；这些命令和自然语言 approval reply 不进入普通聊天 provider loop、不发送普通 `thinking`、不写普通 history
- `/api/v1/notebook/*` 普通 Notebook 列表、统计、创建、详情、更新、删除、记录增删改、带 summary 的 SSE 保存入口
- `/api/v1/question-notebook/*` 题目 Notebook entries、lookup/upsert、分类 CRUD、entry/category 关联与筛选
- `/api/v1/memory*` two-file public memory：`SUMMARY.md`/`PROFILE.md` 快照、单文件保存/清空、从会话刷新、缺失 session/非法 file 错误兼容
- `/api/v1/skills*` file-backed `SKILL.md` 管理：技能 list/detail/create/update/rename/delete、默认 tag 词表、tag create/rename/delete、tag 级联重写、frontmatter scalar 转义、symlink 目录拒绝、前端 JSON DELETE 合同
- `/api/v1/plugins/*` Playground plugins list、tool execute、tool SSE、capability SSE 兼容入口；`web_search` tool 现在读取 active search catalog，`duckduckgo`、`searxng`、`brave`、`tavily`、`perplexity`、`jina`、`serper` provider 会真实调用对应 provider endpoint，并返回 Python-like `content/sources(type=web,title,url,snippet,date,source,content,score,sitelinks,attributes)/metadata(provider,requested_provider,model,search_result_count,provider_metadata)`，不再固定返回 compatibility 文案；capability SSE 现在会把 Playground 请求中的 `config` 和 `attachments` 透传给内部 chat turn，持久化 user message 的 `metadata.request_snapshot` 保留这些字段，避免 `deep_question` 等 capability 丢失模式参数或附件上下文；`deep_question` 已补 Python runtime-only `followup_question_context` 和 `answer_now_context` 入口，follow-up 返回 `mode=followup/question_id` 单题答疑结果，answer-now 返回 `mode=answer_now`、`summary.mode=answer_now`、per-result/top-level `metadata.answer_now=true` 和 `Skipped` content 事件，同时不会把 runtime-only context 写进公开 request snapshot
- `/api/v1/question/generate` 和 `/api/v1/question/mimic` legacy WebSocket 兼容入口：Rust 现在可直接接受旧 Python 首帧请求，`generate` 支持 `requirement/kb_name/count`，发出 `task_id -> status(started) -> progress/templates_ready/question_update/result/progress -> batch_summary -> complete`；`mimic` 支持 `parsed` 与 `upload` 模式，按 Python 先发 `status(init)`，upload 模式补 `status(upload/parsing)`，再发 `status(processing)`、解析/抽取 progress、templates/result 和 `complete`；parsed 模式会读取顶层 `*_questions.json`，映射 `question_text/answer/question_type/question_number/images/reference_question/reference_answer`，`max_questions=0` 会按 Python wrapper 语义默认成 10，负数仍保留“不截断”；parsed 目录没有 `_questions.json`/可抽取内容时返回 `Failed to extract questions from parsed exam`，不再静默生成 fallback 假题；缺 `requirement`、缺 `paper_path`、缺 `pdf_data`、非法 base64、未知 mode 均返回 Python 风格 `error.content`
- `/api/v1/page-agent/openai/v1/chat/completions` Page Agent 会读取 settings catalog 中当前 active LLM；OpenAI-compatible/Azure-compatible profile 会原样转发前端 `messages/tools/tool_choice/temperature/model/extra` 到 `{base_url}/chat/completions` 并返回 provider 原始 JSON，provider 失败时返回 Python 风格 `502 detail=Page-agent LLM request failed: ...`；未配置、deterministic 或非 native-tools provider 保持 `AgentOutput(done)` fallback，保证前端 pet/page-agent 可解析
- `/api/v1/co_writer/documents*` Co-Writer file-backed 文档列表、创建、读取、更新、删除，兼容 12 位 hex id、标题推导、preview、`updated_at` 倒序和 `Document not found` 错误
- `/api/v1/co_writer/edit`、`/api/v1/co_writer/automark`、`/api/v1/co_writer/edit_react`、`/api/v1/co_writer/edit_react/stream` Co-Writer 编辑、自动标注、selection ReAct 普通响应与 SSE 兼容入口；普通 `/edit` 现在会按 Python `EditAgent.process()` 的可审计语义写入 history，保留 `action/source/kb_name/input/output/model`，并在 `source=rag` 或 `source=web` 时写入可由 `/tool_calls/{operation_id}` 回读的 tool call JSON
- `/api/v1/co_writer/history`、`/api/v1/co_writer/history/{operation_id}`、`/api/v1/co_writer/tool_calls/{operation_id}`、`/api/v1/co_writer/export/markdown` Co-Writer 历史、tool call JSON、Markdown 导出兼容入口
- `/api/attachments/{session_id}/{attachment_id}/{filename}` chat attachment preview/download 兼容入口，并兼容旧扫描器看到的四段 alias 形态
- `/api/v1/settings/sidebar/description`、`/api/v1/settings/sidebar/nav-order`、`/api/v1/settings/tour/status|complete|reopen` 旧设置和 tour 入口
- `/api/v1/dashboard/recent`、`/api/v1/dashboard/{entry_id}`、`/api/v1/agent-config/agents*`、`/api/v1/solve/sessions*` legacy dashboard/agent-config/solve session 兼容入口
- `/api/v1/vision/analyze` legacy REST surface：参数校验、无图请求、base64/url 输入优先级、valid data URI metadata-only 降级响应、`image_url` 真实下载、HTTP status/content-type/10MB 大小限制和 unsupported format 错误兼容；当 settings catalog 选中真实 OpenAI-compatible LLM 时，会发送多模态 `text + image_url(data URI)` 请求并执行 bbox / analysis / ggbscript / reflection 四阶段，返回 provider 生成的 `final_ggb_commands`、`ggb_script` 和 `analysis_summary.analysis_mode=provider`
- `/api/v1/vision/solve` legacy WebSocket surface：真实握手、无图 `session -> no_image -> done` 事件，以及 valid image metadata-only `analysis_start -> bbox_complete -> analysis_complete -> ggbscript_complete -> reflection_complete -> analysis_message_complete -> answer_start -> done` 事件兼容；当 settings catalog 选中真实 OpenAI-compatible LLM 时，会复用 REST 的四阶段 Vision 分析，并追加 tutor 文本调用，流式输出 Python 风格 `bbox_complete`、`analysis_complete`、`ggbscript_complete`、`reflection_complete`、`analysis_message_complete`、`answer_start`、`text`、`done`

已运行的验证：

- `cargo fmt --check`：通过
- `cargo test`：`132` 个 API contract 测试 + `5` 个 orchestrator 测试通过
- `cargo clippy --all-targets --all-features -- -D warnings`：通过
- `cargo check --release`：通过
- `python3 tools/api_parity_scan.py --rust-root /home/coobabm/Socartes-Rust --deeptutor-root /home/coobabm/.gitnexus/repos/DeepTutor`：Python missing `0`，frontend missing 仅 `/api/v1/book{param}` 扫描伪影
- `cargo test --test api_contract course`：`6` 个课程/知识库契约测试通过，包含 `/api/v1/course*` 与 `/api/v1/courses*` legacy alias、Python legacy flat course layout 回归
- `cargo test --test api_contract knowledge_routes_read_python_legacy_flat_course_layout -- --exact --nocapture`：`SOCARTES_KNOWLEDGE_ROOT` 直接指向 Python `data/knowledge_bases` 风格目录时，Rust 能从 `{kb}/raw` 列出旧课程文件，并从 `kb_config.json.defaults.default_kb` 恢复默认课程
- `cargo test --test api_contract course_alias_routes_bridge_to_knowledge_contract`：legacy course aliases 列表、默认课程、详情路由契约测试通过
- `cargo test --test api_contract root_endpoint_matches_python_welcome_contract`：根路径 `GET /` 已补齐 Python 欢迎页合同，返回 `{"message":"Welcome to Socartes API"}`
- `cargo test --test api_contract knowledge_detail_route_matches_python_contract`：KB detail 路由、字段和 missing 404 契约测试通过
- `cargo test --test api_contract legacy_chat_and_solve_delete_aliases_match_python_contract`：legacy chat/solve delete alias 返回 Python `status/session_id` 形状的契约测试通过
- `cargo test --test api_contract legacy_chat_ws_accepts_python_payload_and_emits_python_events`：legacy `/api/v1/chat` WebSocket 接受 Python payload，发送 `session/status/stream/sources/result/error` 事件形状且空消息保持连接的契约测试通过
- `cargo test --test api_contract legacy_chat_ws_enable_web_search_emits_python_web_status_and_sources -- --exact --nocapture`：legacy `/api/v1/chat` 的 `enable_web_search=true` 会发送 Python 兼容 `status(stage=web,message="Searching the web...")`，最终 `sources.web` 非空且 `sources.rag` 为空
- `cargo test --test api_contract plugin_web_search_uses_configured_searxng_provider_like_python -- --exact --nocapture`：plugin `web_search` 会按 active SearXNG profile 调用真实 provider endpoint，解析 `title/url/content/engine` 为 Python-like content、sources 和 metadata，不再返回 compatibility stub
- `cargo test --test api_contract plugin_web_search_uses_configured_duckduckgo_provider_like_python -- --exact --nocapture`：plugin `web_search` 会按 active DuckDuckGo profile 调用真实 DuckDuckGo HTML endpoint，解析 Python DDGS row shape 的 `title/href/body` 为 `citations/search_results/sources`，source 固定 `DuckDuckGo`，metadata 不再带 compatibility stub 标记
- `cargo test --test api_contract plugin_web_search_uses_configured_brave_provider_like_python -- --exact --nocapture`：plugin `web_search` 会按 active Brave profile 调用真实 Brave Search endpoint，校验 `q/count` 参数、`Accept`/`X-Subscription-Token` headers，并解析 `web.results[].title/url/description/age` 为 Python-like content、sources、citations 和 metadata，不再返回 compatibility stub
- `cargo test --test api_contract plugin_web_search_uses_configured_tavily_provider_like_python -- --exact --nocapture`：plugin `web_search` 会按 active Tavily profile 调用真实 Tavily endpoint，校验默认 body、直接使用 Tavily answer，并解析 `results[].content/raw_content/score/published_date` 和 `images/response_time` 为 Python-like content、sources、citations 和 metadata，不再返回 compatibility stub
- `cargo test --test api_contract plugin_web_search_uses_configured_jina_provider_like_python -- --exact --nocapture`：plugin `web_search` 会按 active Jina profile 调用真实 Jina endpoint，校验 Bearer/enrich headers、Python `quote()` 路径编码、usage token 汇总，并把 enriched attributes 暴露在 `sources[].attributes`
- `cargo test --test api_contract plugin_web_search_uses_configured_serper_provider_like_python -- --exact --nocapture`：plugin `web_search` 会按 active Serper profile 调用真实 Serper endpoint，校验 `X-API-KEY`、`q/num/gl/hl/page/autocorrect` body，并解析 `organic[]`、`answerBox`、`knowledgeGraph`、PAA、related searches 和 result `sitelinks/attributes`
- `cargo test --test api_contract legacy_solve_ws_accepts_python_payload_emits_result_and_closes`：legacy `/api/v1/solve` WebSocket 接受 Python payload，发送 `session/task_id/status/agent_status/result` 并关闭连接的契约测试通过
- `cargo test --test api_contract legacy_solve_ws_uses_python_task_id_and_provider_token_stats -- --exact --nocapture`：legacy `/api/v1/solve` 在 settings catalog 选中 mock OpenAI-compatible LLM 时会真实调用 `/v1/chat/completions`，`task_id` 使用 Python `solve_...` 格式，并把 provider `usage` 映射为非零 `token_stats.tokens/input_tokens/output_tokens`
- `cargo test --test api_contract legacy_solve_ws_writes_python_style_output_dir_and_final_answer_file -- --exact --nocapture`：legacy `/api/v1/solve` 成功 result 会返回非空 `output_dir/output_dir_name`，在 Python 风格 solve 输出目录写入 raw `final_answer.md`，并暴露 `metadata.output_dir/total_steps/completed_steps/plan_revisions`
- `cargo test --test api_contract legacy_solve_ws_rewrites_artifact_links_to_public_output_urls_like_python -- --exact --nocapture`：legacy `/api/v1/solve` result 会把 `](artifacts/...)` 链接改写成 `/api/outputs/workspace/chat/deep_solve/<solve_dir>/artifacts/...`，同时落盘 markdown 保留 raw 相对链接
- `cargo test --test api_contract legacy_solve_ws_defaults_to_python_deep_solve_tools_and_ai_textbook -- --exact --nocapture`：legacy `/api/v1/solve` 不传 `tools/kb_name` 时会按 Python 默认启用 `rag/web_search/code_execution/reason`，并把 turn snapshot/session preferences 里的默认课程设为 `ai-textbook`
- `cargo test --test api_contract quiz`：`4` 个 quiz/session 契约测试通过，覆盖空 quiz results 返回 `400`、写回 `[Quiz Performance]` deep_question 消息、question notebook upsert，以及 PB-only session 的 quiz-results 持久化不写本地 session JSON
- `cargo test --test api_contract quiz_results_records_deep_question_message_and_upserts_question_notebook_like_python`：quiz results Python 写回语义定向契约测试通过
- `cargo test --test api_contract auth`：`4` 个 auth 契约测试通过，覆盖 auth disabled、首用户 admin 注册、bcrypt login、JWT cookie/Bearer status、admin 用户创建/列表/改角色/删除、自我保护、普通 user admin 403，以及 auth-enabled 业务 HTTP 全局 guard
- `cargo test --test api_contract pocketbase_auth_mode`：`10` 个 PocketBase auth/session 契约测试通过，覆盖 raw PB token login cookie、`auth-refresh` status/protected HTTP guard、PB first-user register non-admin shape、admin create user proxy 到 PB `users` collection 且不写本地 users store，PB-backed chat turn 四集合持久化、session detail/list 聚合、rename、cascade delete、不写本地 session JSON，PB `messages.metadata_json` schema 拒绝时返回 502 且清理本次半写，PB-only session quiz-results 写入 PB message + question notebook，以及 PB-backed unified WebSocket running-turn partial persistence、PB replay + live continuation、terminal upsert 去重、stored running turn cancel terminal replay、PB-backed regenerate 复用 PB session/user message 并替换尾部 assistant
- `cargo test --test api_contract pocketbase_auth_mode_chat_turn_rolls_back_pb_half_writes_when_message_metadata_schema_rejects -- --exact --nocapture`：模拟 PB 拒绝 assistant `messages.metadata_json` 时，internal test chat turn 返回 `502`，并确认 `sessions/messages/turns/turn_events` 不留下本次 terminal 半写
- `cargo test --test api_contract auth_enabled`：`10` 个 auth-enabled 标准 JWT/本地用户契约测试通过，确认 PB 分支没有破坏 legacy users/secret 迁移、env-admin fallback、HTTP/WS guard、settings 裁剪与 auth-scoped RAG
- `cargo test --test api_contract auth_enabled_global_guard_protects_business_http_routes_like_python`：auth-enabled 全局 HTTP guard 定向契约测试通过，覆盖 `/api/v1/knowledge/list`、`/api/v1/course(s)`、settings、sessions、attachments 未登录 401，docs/auth bootstrap/outputs 公开，以及 Bearer/cookie 登录后访问
- `cargo test --test api_contract settings_llm_options`：`2` 个 `/api/v1/settings/llm-options` 契约测试通过，覆盖 admin catalog-driven active/default/options/context_window 字段，以及 auth-enabled 普通用户无 grant 空列表、有 grant 仅返回分配模型且不泄露运行时 secret
- `cargo test --test api_contract auth_enabled_`：`10` 个 auth-enabled HTTP/WS 鉴权定向契约测试通过；覆盖 legacy users/secret 迁移、env-admin fallback 不落盘、全局 HTTP guard、settings 角色裁剪与 admin-only catalog/apply、统一 `/api/v1/ws` query token / `dt_token` cookie / 4001 close 行为、router-level WebSocket 路由 Bearer/cookie 握手鉴权和未登录 HTTP 401 拒绝行为，以及 unified WS/legacy solve 的 auth-scoped RAG 不越权
- `cargo test --test api_contract auth_enabled_unified_ws_rag_rejects_unassigned_admin_kb_like_python`：auth-enabled 普通用户通过 unified `/api/v1/ws` 请求未分配 admin KB 时返回 terminal error，且事件流不泄露 admin KB 内容
- `cargo test --test api_contract auth_enabled_legacy_solve_without_kb_does_not_fallback_to_builtin_for_user`：auth-enabled 普通用户通过 legacy `/api/v1/solve` 启用 RAG 但不传 `kb_name` 时返回 no-KB-selected 错误，不再 fallback 到内置/admin 课程
- `cargo test --test api_contract multi_user_`：`4` 个 multi-user 契约测试通过；覆盖 `/me/access`、`/admin/resources`、`GET|PUT users/{id}/grants`、secret/path grant 拒绝、target admin 403、`/users` admin-only 列表、`spaces/assign` 的 path escape/missing source/duplicate target/复制 provenance/grant.spaces 副作用，以及 auth-enabled 普通用户 KB/Skill 隔离、assigned admin KB 只读、plugin RAG 无 KB 不 fallback、assigned admin skill 与 user shadow 行为
- `cargo test --test api_contract knowledge_progress_ws_streams_initial_reindex_task_update`：课程 progress WebSocket 可连接并按 reindex `task_id` 推送 `type=progress` / `stage=completed` / `percent=100` 契约测试通过
- `cargo test --test api_contract course_knowledge_mutation_workflow_matches_frontend_contract -- --exact`：课程 create/default/reindex/delete 前端合同测试通过，并覆盖 Python 风格 `kb_init_` / `kb_reindex_` task id、task stream `process_log/progress/complete` payload、未知 task 返回空 SSE 流而不是 JSON 404
- `cargo test --test api_contract knowledge_python_config_progress_and_linked_folder_endpoints_match_contract -- --exact`：课程 config/progress/linked-folder/sync 契约测试通过，覆盖 create progress 带 task id、reindex 清理 `needs_reindex` 后 sync-folder 返回 `kb_upload_` task id
- `cargo test --test api_contract knowledge_sync_folder_rejects_needs_reindex_without_starting_task_like_python -- --exact`：linked-folder sync 在 KB 标记 `needs_reindex` 时返回 Python 风格 `409`，不启动后台 upload task
- `cargo test --test api_contract knowledge_sync_folder_without_active_index_returns_task_then_failed_like_python -- --exact`：linked-folder sync 在 KB 未标记 `needs_reindex` 但没有 active index 时先返回 `kb_upload_` task id，然后后台按 Python upload worker 语义失败，且不复制 linked 文件、不更新 sync state、不写 `last_indexed_action`
- `cargo test --test api_contract knowledge_reindex_failure_returns_task_and_failed_sse_like_python_background_task -- --exact`：reindex 后台失败时 replay Python 形状 error process logs、`progress.stage=error`、`failed.detail` 原始错误和 `failed.details` traceback 诊断
- `cargo test --test api_contract knowledge_create_returns_before_slow_background_indexing_like_python_init -- --exact --nocapture`：课程 create 在 embedding provider 延迟 2 秒时仍能在 500ms 内返回 `kb_init_...` task id，随后 task stream replay `process_log/progress/complete`，并生成 active chunk index
- `cargo test knowledge_task_ -- --nocapture`：Knowledge task stream runtime 单元测试通过，覆盖订阅后 live event 推送、backlog 只保留最近 500 条、`failed` payload replay 并结束 stream
- `cargo test --test api_contract knowledge_reindex_failure_returns_task_and_failed_sse_like_python_background_task -- --exact --nocapture`：课程 reindex 无可抽取内容时返回 `200 + kb_reindex_... task_id`，progress 进入 `stage=error`，task SSE replay `process_log/progress/failed` 且不伪造 `complete`
- `cargo test --test api_contract knowledge_reindex_task_stream_replays_queueing_progress_before_terminal_event -- --exact --nocapture`：课程 reindex task stream 会先 replay `Queueing re-index...` 的 `stage=starting` progress，再以 `complete` 终止，并且成功路径不发送 `progress stage=completed`
- `cargo test --test api_contract knowledge_reindex_task_stream_replays_embedding_batch_progress_before_terminal_event -- --exact --nocapture`：课程 reindex task stream 会在 queueing progress 和 terminal complete 之间 replay Python 风格 `stage=processing_documents` / `Embedding batches:` 进度
- `cargo test --test api_contract knowledge_reindex_returns_before_slow_embedding_worker_completes_like_background_task -- --exact --nocapture`：课程 reindex 在 embedding provider 延迟 2 秒时仍能在 500ms 内返回 `task_id`，证明索引重活已移入后台 worker
- `cargo test --test api_contract reindex -- --nocapture`：`8` 个 reindex 相关契约测试通过，覆盖成功、失败、progress WS、Office 文档 reindex 和慢 embedding 后台返回
- `cargo test --test api_contract knowledge_sync_folder_tracks_state_and_skips_unchanged_files -- --exact --nocapture`：linked-folder sync 状态契约测试通过，覆盖首次 sync 记录 `last_sync/synced_files`，未修改文件二次 sync no-op，文件修改后按 `modified_files=1` 重新同步
- `cargo test --test api_contract knowledge_sync_folder_updates_active_chunk_index_like_python_upload -- --exact --nocapture`：linked-folder sync 后会重写 active chunk index，plugin RAG 能检索并引用同步进来的文件 source
- `cargo test --test api_contract knowledge_sync_folder_returns_before_slow_background_indexing_like_python_upload -- --exact --nocapture`：linked-folder sync 在 embedding provider 延迟 2 秒时仍能在 500ms 内返回 `kb_upload_...` task id，并通过 task SSE 后台完成
- `cargo test --test api_contract knowledge_upload_rejects_kb_that_needs_reindex_like_python -- --exact`：课程 upload 仍按 Python 规则拒绝 missing/stale index，并在 reindex 后返回 `kb_upload_` task id 和上传文件列表
- `cargo test --test api_contract knowledge_upload_returns_before_slow_background_indexing_like_python -- --exact --nocapture`：课程 upload 在 embedding provider 延迟 2 秒时仍能在 500ms 内返回 `kb_upload_...` task id，随后 task stream replay `process_log/progress/complete` 且 RAG 可检索新文件
- `cargo test --test api_contract knowledge_reindex_creates_signature_version_and_reports_active_match`：课程 reindex signature/version/noop/active_match 契约测试通过
- `cargo test --test api_contract knowledge_reindex_persists_chunk_index_and_rag_uses_indexed_chunks`：课程 reindex 持久化 chunk index、plugin RAG 和 chat sources 使用 chunk 级 source 的契约测试通过
- `cargo test --test api_contract knowledge_rag_uses_embedding_similarity_over_keyword_overlap`：课程 RAG 会按 embedding/cosine similarity 选中语义匹配 chunk，而不是按关键词重叠选中 decoy chunk；同时验证 `chunks.json` 和 `default__vector_store.json.embedding_dict` 写入向量
- `cargo test --test api_contract knowledge_file_preview_serves_inline_mime_and_preserves_subdirectories_like_python`：课程原文件预览契约测试通过，覆盖 PDF/PNG `Content-Type`、`inline` disposition、子目录文件读取和路径逃逸 `403`
- `cargo test --test api_contract office_documents_upload_reindex_and_rag_extract_text`：DOCX/XLSX/PPTX 上传、文件 MIME、reindex 后 chunk 文本抽取、sheet/slide marker、plugin RAG source 回指 Office 文件的契约测试通过
- `cargo test --test api_contract corrupt_parser_documents_are_skipped_instead_of_indexing_binary_garbage`：坏 DOCX/PDF parser 文件会被跳过，混合 KB 仍索引好文本文件且不写入坏 parser source 的契约测试通过
- `cargo test --test api_contract bad_parser_file_like_python_document_adder -- --nocapture`：upload 与 linked-folder sync 遇到坏 DOCX parser 文件时按 Python `DocumentAdder` 语义完成后台 task，`Successfully processed 0 files!`，不更新 `file_hashes`，sync-folder 不把坏文件记入 `synced_files`
- `cargo test --test api_contract knowledge_upload`：`2` 个课程上传契约测试通过，覆盖 missing/stale active index 返回 `409`、上传后 active chunk index 可检索、`file_hashes`/`update_history` 写入，以及重复内容不写入索引也不改 metadata
- `cargo test --test api_contract rag_selected_uploaded_kb_returns_no_builtin_fallback_when_no_match`：选定上传课程无命中时不回退内置 RAG 的语义契约测试通过
- `cargo test knowledge_python_config_progress_and_linked_folder_endpoints_match_contract`：Python-only knowledge 管理兼容端点契约测试通过
- `cargo test --test api_contract attachment_preview_route_serves_local_chat_files_like_python_contract`：chat attachment preview/download 契约测试通过
- `cargo test --test api_contract chat_ws_start_turn_injects_text_attachment_into_selected_llm`：selected LLM turn 的 text/base64 附件落盘、文本注入 provider user message、持久化清空 base64 和 `/api/attachments/...` 回读契约测试通过
- `cargo test --test api_contract chat_ws_start_turn_preserves_selected_llm_usage_and_reasoning_metadata`：selected LLM chat completion 的 visible answer、`usage`、`reasoning_content`、`finish_reason` 解析和持久化契约测试通过，hidden reasoning 不进入 assistant content
- `cargo test --test api_contract chat_ws_start_turn_uses_responses_api_for_gpt5_reasoning_selection`：selected GPT-5/reasoning LLM 会调用 `/v1/responses`，发送 Responses `instructions/input/reasoning/include/store:false` 请求形状，并把 Responses visible answer、reasoning summary 和 token usage 映射回 assistant metadata
- `cargo test --test api_contract system_test_llm_uses_responses_api_for_gpt5_reasoning_selection`：system LLM probe 对 GPT-5/reasoning catalog selection 使用 `/v1/responses`，并通过 raw helper 转成 chat-compatible JSON 供现有 settings/system/page-agent 解析路径复用
- `cargo test --test api_contract chat_ws_start_turn_falls_back_from_responses_api_on_unsupported_error`：selected GPT-5/reasoning chat turn 在 Responses API 返回 unsupported/Responses 400 时自动重试 `/chat/completions`，最终以 fallback provider answer 完成
- `cargo test --test api_contract system_test_llm_falls_back_from_responses_api_on_unsupported_error`：system LLM probe 在 Responses API 返回 unknown-parameter/unsupported 422 时自动 fallback 到 `/chat/completions` 并返回 `success:true`
- `cargo test --test api_contract system_status_search_reports_`：`/api/v1/system/status` search 状态契约测试通过，覆盖 `duckduckgo` configured、`tavily/serper` 带 key configured、`brave` 缺 key fallback 到 `duckduckgo`、`openrouter` unsupported、`perplexity/serper` 缺 key not_configured，以及 Python 对齐 error 文案
- `cargo test --test api_contract book_creation_and_editing_workflow_matches_frontend_contract`：Book supplement 现在断言 Python 的 callout/text/quiz 三连追加和返回 quiz block 语义
- `cargo test --test api_contract book_health`：Book health drift/log parser 契约测试通过，覆盖 changed KB fingerprint、ready page stale 持久化、`current_fingerprints`、Python-style `log.md` failure/repeated signature 统计
- `cargo test --test api_contract book_refresh_fingerprints_recomputes_hash_clears_stale_and_appends_log_like_python`：Book refresh fingerprints 契约测试通过，覆盖真实 SHA-256 fingerprint、清空 stale markers、manifest 持久化和 `kb_health` log append
- `cargo test --test api_contract book_frontend_bootstrap_reads_file_backed_books_and_outputs`：Book file-backed bootstrap/health/refresh smoke contract 在真实 fingerprint 行为后仍通过
- `cargo test --test api_contract legacy_settings_dashboard_agent_config_and_solve_routes_match_python_contracts`：legacy settings/dashboard/agent-config/solve 契约测试通过
- `cargo test --test api_contract vision_`：`5` 个 Vision REST/WebSocket 校验、REST image URL 下载/content-type 错误、真实 WS 握手、metadata-only 降级事件序列、OpenAI-compatible 多模态 provider 四阶段分析和 tutor 文本契约测试通过
- `cargo test --test api_contract chat_ws_`：`22` 个 unified chat WebSocket 测试通过；覆盖 Python/frontend `metadata.request_snapshot` 持久化、regenerate 从上一条 user snapshot 回放 `llmSelection` + `bookReferences` 并注入 `[Book Context]`、regenerate 无效 selection 不破坏历史、regenerate 不重复 user message、PB-backed regenerate 复用 PB session/user message 并替换尾部 assistant、GPT-5/reasoning Responses API 非流式路径、selected LLM native tool-call RAG，以及 auth-scoped RAG 拒绝未分配 admin KB 等目标用例
- `cargo test --test api_contract chat_selected_llm_tool_calls_are_normalized_truncated_and_bound_to_selected_course`：selected LLM native tool call 的 id 归一化、最多 4 个 tool call 截断、第二轮禁用 tools、`rag.kb_name` 强制绑定 selected course、malformed arguments 回退用户问题的契约测试通过
- `cargo test --test api_contract embedding_test`：`9` 个 system/settings embedding diagnostic 契约测试通过，覆盖不可达 provider 失败、system 固定 Python batch probe、Azure `api-version`/`api-key`/`extra_headers`、Cohere v2 payload/response、Ollama payload/response、settings detected dimension 写回且不发送 `dimensions`、已知模型能力表刷新、provider 失败 SSE、空 vector terminal failed 文案
- `cargo test --test api_contract settings_`：`10` 个 settings 契约测试通过，覆盖 role-aware `GET /settings`、普通 user 禁止 catalog/apply 旁路、admin catalog/apply 脱敏与 masked-secret restore、settings LLM diagnostic 命中 provider、Authorization/extra_headers 透传、probe prompt、`response/context_window/catalog/completed` SSE、context window 写回 catalog、settings search SSE 成功形状和 search start 空 body
- `cargo test --test api_contract search`：`26` 个 search/status diagnostic 契约测试通过，覆盖 system/status configured/fallback/unsupported/not_configured、system test unsupported provider、perplexity/serper missing credentials、brave missing key fallback 到 duckduckgo、system test DuckDuckGo/Tavily/Perplexity/Serper provider-backed 成功、settings search SSE 成功形状、settings search start 空 body，以及 DuckDuckGo/SearXNG/Brave/Tavily/Perplexity/Jina/Serper provider-backed plugin execution
- `cargo test skills_`：`5` 个 Skills API 契约测试通过
- `cargo test --test api_contract deep_question_execute_stream_honors_followup_and_answer_now_runtime_contexts`：`deep_question` follow-up/answer-now runtime-only context 契约测试通过，覆盖 `mode=followup/question_id`、answer-now `summary.results`/`metadata.answer_now`、`Skipped` content 和 request snapshot 不泄露 `answer_now_context`
- `cargo test --test api_contract question_mimic_zero_max_questions_defaults_to_ten_like_python`：legacy mimic parsed `max_questions=0` 按 Python wrapper 语义归一为 10，并截断超过 10 条 reference questions
- `cargo test --test api_contract question_mimic_parsed_without_extracted_questions_errors_like_python`：legacy mimic parsed 缺 `_questions.json`/可抽取内容时返回 Python 风格 extraction failure，不再落到 deterministic fake quiz fallback
- `cargo test --test api_contract question_mimic_parsed_invalid_questions_json_uses_python_error_text`：legacy mimic parsed 顶层 `_questions.json` 无效 JSON 时返回 Python `JSONDecodeError` 风格错误，不发 fallback result
- `cargo test --test api_contract question_mimic_parsed_empty_questions_emits_unknown_error_like_python`：legacy mimic parsed `questions: []` 时发出 `templates_ready(count=0)` 与 `progress(complete)`，最终按 Python 路由兜底返回 `Unknown error`
- `cargo test plugins_`：`4` 个 Playground plugins API 契约测试通过
- `cargo test --test api_contract plugins_capability_stream_forwards_config_and_attachments_into_turn_request_snapshot`：Playground capability stream 的 `config`/`attachments` 透传和 request snapshot 持久化契约测试通过
- `cargo test --test api_contract question_`：`5` 个 question/deep_question 契约测试通过，覆盖 `deep_question` capability、legacy `/api/v1/question/generate` 正常事件序列、legacy `/api/v1/question/mimic` parsed 正常事件序列、Python 风格 validation errors，以及 question notebook CRUD/分类
- `cargo test --test api_contract question_generate_websocket_matches_python_contract`、`question_mimic_websocket_matches_python_status_contract`、`question_websockets_return_python_validation_errors`：legacy question WebSocket 三个定向契约测试通过
- `cargo test --test api_contract page_agent`：`4` 个 Page Agent 契约测试通过，覆盖 fallback、messages 校验、OpenAI-compatible provider 转发和 provider 失败 `502`
- `cargo test --test api_contract co_writer`：`4` 个 Co-Writer 文档、普通 edit history/tool-call、编辑/自动标注/stream、历史/tool-calls/export 契约测试通过
- `cargo test --test api_contract book_`：`23` 个 broad `book_` 过滤契约测试通过，其中覆盖 Book WS create Python event shape、Book WS confirm/compile/regenerate Python event/result shape、ready/no-force compile WS 幂等、force compile 保留现有非 overview block 计划并清理 generated outputs、force compile 保留 ready overview blocks、overview 只生成非 ready blocks、缺失 `concept_graph` 参数的 overview graph block 编译/重生成失败元数据、successful concept graph generator 路径按 Python 语义忽略 `params.chapter_index` 并输出空 `payload.index.chapters`、Python page status/error 聚合、overview `ConceptGraphBlock` payload、非 overview compile-page Python-like 多 block 静态规划、deep-dive parent link/block metadata/synthetic chapter、quiz attempt weak chapter/integer score、regenerate Book Context replay 相关用例
- Chromium 本地打开 `http://127.0.0.1:3011/book?book=<id>`：页面 `200`，reader 正文渲染，控制台 `0` 个 error
- Chromium 本地打开 `http://127.0.0.1:3011/settings`：页面 `200`，settings/status/catalog 渲染，控制台 `0` 个 error
- Chromium 本地打开 `http://127.0.0.1:3011/agents`：页面 `200`，TutorBot 列表和 tabs 渲染，控制台 `0` 个 error
- Node WebSocket 直连 `ws://127.0.0.1:8810/api/v1/tutorbot/<id>/ws`：收到 `thinking,content,done`
- `cargo test --test api_contract tutorbot_ws_restart_command_returns_python_message_without_mutating_history`：TutorBot `/restart` Python 兼容契约测试通过，覆盖固定 `Restarting...` 文案、不调用 provider、不写 slash history、保留已有 canonical history
- `cargo test --test api_contract tutorbot_ws_usage_slash_commands_return_python_text_without_provider_or_history`：TutorBot 空 `/btw` 和精确 `/team` usage 文案契约测试通过，覆盖不调用 provider、不写普通 history
- `cargo test --test api_contract tutorbot_ws_btw`：`2` 个 TutorBot `/btw` 契约测试通过，覆盖 Python accepted 文案、8 位 hex task id、不进入普通聊天 thinking、不写普通 history，以及后台 selected provider 调用完成后通过 WebSocket `type=proactive` 主动通知
- `cargo test --test api_contract tutorbot_ws_btw_instruction_runs_background_provider_and_sends_proactive_result_like_python -- --exact`：TutorBot 带参数 `/btw <instruction>` 会立即 accepted/done，然后异步调用 selected OpenAI-compatible provider，并把结果作为 `type=proactive` 推回当前 WS
- `cargo test --test api_contract tutorbot_ws_stop_cancels_active_btw_background_task_like_python -- --exact`：TutorBot `/stop` 会取消当前 bot 的 active `/btw` 后台任务，返回 `Stopped N task(s).`，并抑制已取消任务完成后的 `type=proactive` 推送
- `cargo test --test api_contract tutorbot_ws_team_idle_commands_return_python_text_and_metadata_only_session`：TutorBot 空闲 `/team status` 和 `/team stop` 契约测试通过，覆盖 Python 文案、metadata-only session 更新、不调用 provider、不写普通 history
- `cargo test --test api_contract tutorbot_ws_team_goal_starts_runtime_status_log_and_stop_like_python -- --exact`：TutorBot `/team <goal>` runtime 契约测试通过，覆盖 Python web lead update、`team_text` ack、runtime 文件树、`config/tasks/events` 字段 shape、`status/log/stop`、session metadata 和普通 history 不变式
- `cargo test --test api_contract tutorbot_ws_team_goal_uses_provider_generated_plan_when_llm_selected -- --exact`：TutorBot `/team <goal>` 在 bot 配置 selected LLM 时会调用 provider 生成初始 team plan，落盘 provider members/tasks、`config.planning`、`team_started.data.plan_source=provider`，并保持普通 history 为空
- `cargo test --test api_contract tutorbot_ws_team_goal_repairs_malformed_provider_plan_like_python -- --exact`：第一次 provider plan malformed 时会第二次调用 provider，repair prompt 带 `Malformed JSON.`，第二次有效 JSON fence 会落盘 provider `mission/tasks/notes`
- `cargo test --test api_contract tutorbot_ws_team_goal_falls_back_after_two_invalid_provider_plans_like_python -- --exact`：两次 provider plan 无效后回退 Python fallback 2 worker/2 task/notes 形状，并保持 provider 请求次数为 2
- `cargo test --test api_contract tutorbot_ws_team_goal_normalizes_invalid_owner_and_dependencies_like_python -- --exact`：provider plan 中不存在的 owner 写为 JSON null，未知 dependencies 被过滤，空白 notes 默认 `# Team Notes\n`
- `cargo test --test api_contract tutorbot_ws_team_goal_repair_prompt_includes_validation_error_like_python -- --exact`：duplicate task ids validation error 会原样进入第二次 repair prompt
- `cargo test --test api_contract tutorbot_ws_team_goal_retries_cyclic_dependency_graph_like_python -- --exact`：任务依赖图成环会触发第二次 repair，并在 prompt 中包含 `task dependency graph has a cycle`
- `cargo test --test api_contract tutorbot_ws_team_approval_commands_update_runtime_like_python -- --exact`：TutorBot `/team approve|reject|manual` 契约测试通过，覆盖缺参 usage、status approval queue、tasks 状态/result 写回、events.jsonl、manual mailbox、`team_text` metadata 和普通 history 不变式
- `cargo test --test api_contract tutorbot_`：TutorBot 契约测试覆盖管理/souls/profile 文件、WS 选中 provider + RAG tool-call loop + history 持久化、Python-style bot canonical session 历史、Python JSONL metadata header、`/help`、`/stop`、`/restart` slash command 不写历史/不调用 provider、`/new` 先归档旧 session 到 `SUMMARY.md` 再清空 canonical session，以及 nano team runtime/approval 命令

## 2026-05-30 CLI 兼容快照

- Python 当前主副本入口是 `socartes = socartes_cli.main:main`，真实命令树为 `run/start/serve/chat/book/bot/kb/notebook/memory/plugin/config/session/provider`；旧 live/GitNexus 副本仍保留 `deeptutor = deeptutor_cli.main:main`，命令树与已改名后的 `socartes_cli` 相同。
- Rust CLI 已暴露 `socartes`、`socartes-cli`、`socartes_cli`、`deeptutor`、`deeptutor-cli`、`deeptutor_cli` 六个 bin；六者共享同一命令面，契约测试覆盖 `run/start/serve/chat/book/bot/kb/notebook/memory/plugin/config/session/provider/init` help surface。注意 Python 原始 `socartes_cli`/`deeptutor_cli` 没有 `init`/`wizard` 命令；Rust `init` 是新增 initializer，不是旧 CLI 迁移证据。
- `cargo test --test cli_contract` 现有 `45` 条 CLI 契约，覆盖 `socartes-cli`/`socartes_cli` alias、旧 `deeptutor`/`deeptutor-cli`/`deeptutor_cli` alias、`bot create`、`book health`、`memory clear`、`kb search`、`kb create --docs-dir`、`kb add --docs-dir`、`session open`、`notebook add-md`、`plugin info`、`config show` 的 Python `main.yaml system.language` 语义、`init wizard`、`start` stale launcher state cleanup 和端口占用启动前诊断的真实 path/payload 或文件副作用，不再只证明子命令 help 存在。
- `init` 现在额外写 Python `scripts/start_tour.py` / `EnvStore` 兼容的根目录 `.env` 与 `data/user/settings/interface.json`：端口、LLM、Embedding、Search 使用 Python 键名 `BACKEND_PORT/FRONTEND_PORT/LLM_HOST/EMBEDDING_HOST/...`，UI 语言写入 `interface.json`，使后续 `start`/launcher-style 入口可以读取同一配置真值。
- 工程化缺口已开始补齐：仓库根目录新增 Rust-only `Dockerfile` / `.dockerignore`，镜像包含 `socartes-backend` 服务端和六个 CLI 兼容二进制，默认 `PORT=8000`、`SOCARTES_DATA_DIR=/app/data`，并用 `/health` 做 Docker healthcheck；`.github/workflows/ci.yml` 会在 push/PR 上跑 `cargo fmt --check`、`cargo clippy --all-targets -- -D warnings`、`cli_contract`、`api_contract`、`orchestrator_contract` 和 Docker build；`.github/workflows/docker-release.yml` 会在 GitHub Release 发布时构建并推送 GHCR 多架构镜像。
- CLI 端到端真实后端契约已新增两条：一条启动真实 Rust `socartes_backend::app_with_knowledge_root_and_auth(..., false)` 后通过 CLI 完成 `notebook create/add-md/show`、`memory show/clear`、`plugin info rag`；另一条通过 CLI 完成 `kb create`、等待 Python 风格 `kb_init_...` task stream `complete`、再用 `kb list/search` 验证真实 Rust RAG 索引可检索上传文档。该覆盖专门补上之前 mock HTTP route 只能证明“发到某个 URL”、不能证明 CLI 与 Rust 后端真实联动的缺口。
- Rust CLI 默认端口已按 Python launcher 对齐：全局 `--api-url` 默认 `http://127.0.0.1:8001`，`serve --port` 默认 `8001`，本地 `config show --home` 在没有 `ui.json` 时返回 `backend=8001/frontend=3782`，避免资源命令默认打到旧 `8000` 端口。
- `memory show|clear` 现在在 API 连接失败或超时时会回退到本地 `SOCARTES_MEMORY_ROOT` 或 `SOCARTES_DATA_DIR/memory`，可无服务读写 `SUMMARY.md` / `PROFILE.md`，补齐 Python CLI 直接调用本地 memory service 的一部分语义。
- `plugin info` 现在会同时查 `/api/v1/plugins/list` 返回的 `plugins`、`tools`、`capabilities` 三段；`deep_research` 这类 playground plugin manifest 不再被误报为找不到。
- `kb create/add --docs-dir` 已按 Python `FileTypeRouter.collect_supported_files(..., recursive=True)` 的 extension 规则过滤，只上传 PDF/Office/text-like 文件；`.png/.jpg` 图片和 `.bin` 等不支持文件不会进入 CLI multipart 上传；`kb add` 已锁定到 `/api/v1/knowledge/{name}/upload`，不再只覆盖 create 路径。
- `chat` REPL 已补 Python 风格状态变更回显：`/tool on|off`、`/cap`、`/kb`、`/history add|clear`、`/notebook add|clear`、`/config clear|set` 更新本地状态后会立即打印当前 state，并确认下一轮 turn payload 携带这些变更。
- `socartes start` 已不再等价于 backend `serve`：Rust CLI 现在会生成 backend `serve` 子进程 + Next frontend `npm run dev -- --port <frontend_port>` 的启动计划，按 Python `start_web.py` 语义读取 `.env -> process env -> default(8001/3782)` 的端口与 auth 开关，并在 dry-run 契约中验证 `NEXT_PUBLIC_API_BASE`、frontend cwd 和 `data/user/settings/start_web_state.json` state path；非 dry-run 路径会在启动前清理同一 state 文件记录的 stale backend/frontend pid/pgid，先检测 backend/frontend 端口占用并输出 Python 风格 `port is already in use`、owner/unknown owner 与 `stop_web.py` hint，写 Python-compatible state schema `version/created_at/backend_port/frontend_port/processes.{backend,frontend}.{pid,pgid}`，backend 或 frontend readiness 失败时按 `frontend -> backend` 清理已启动进程并删除 state，Unix 路径使用新进程组并以 TERM grace -> KILL 终止树。
- 仍未完成的 CLI 高风险差距：Rust `start` 的 Windows taskkill 树状终止还未补；Rust CLI 大多数命令仍是 HTTP API client，而 Python CLI 多数命令直接调用本地 `SocartesApp`/服务对象，离无服务本地执行的 drop-in 替代还差一层本地 app/runtime 执行模型；`plugin info` 已覆盖插件清单段但信息深度仍取决于 `/api/v1/plugins/list` 返回形状，不像 Python 直接查本地 registry；`config show` 默认读 Rust `data/settings/{catalog,ui}.json`，不是 Python 的 env/main.yaml/runtime resolver；`memory` 已有本地 fallback 但还没有复刻 Python Rich markdown panel 渲染；`serve --reload` 仍只是兼容参数，不是真热重载。

## 2026-05-30 Chat Streaming 与 Book Runtime 增量

- Python unified WS 的 selected LLM + native tool call 语义经只读审计确认：tool-call planning 请求本身是非流式，RAG/tool 执行通过 Socartes 事件流报告进度，工具结果注入后最终回答才走 provider streaming。Rust 新增合同 `chat_ws_start_turn_streams_final_chunks_after_selected_llm_tool_call`，锁定第一段 `stream=false/tools/tool_choice=auto`、第二段 `stream=true/stream_options.include_usage=true`、RAG tool result 注入 second request、最终 WebSocket 输出两个 `content` chunk。
- Deep Research confirmed outline 二阶段已补：无 `confirmed_outline` 仍返回 Python 风格 outline preview；非空 `confirmed_outline` 现在跳过 preview，发出 `researching -> reporting` 阶段事件，按已确认 section 顺序生成 `response/content/metadata.confirmed_outline_used/sub_topics/research_config`，合同 `deep_research_execute_stream_with_confirmed_outline_runs_report_contract` 覆盖该路径。
- Book runtime 新增同页 active compile claim：`AppState.book_runtime.compiling_pages` 会让同一 `book_id/page_id` 的并发 compile 请求共享首个 active compile；等待者在首个请求完成后读取已写入 page，不再重复生成和写日志。合同 `book_compile_page_concurrent_force_requests_share_active_claim_like_python` 先确认旧行为会对 8 个并发 force compile 写 8 条 `compile_page` log，再验证现在只写 1 条，覆盖 Python `BookEngine._claim_page_compile` 的核心并发语义。
- Book 普通页 `compile-page` 现在会在 settings catalog 选中真实 OpenAI-compatible LLM 时走 selected provider 优先路径：非 overview 页会把 book/chapter/page/objectives/knowledge_bases/source anchors 写入 provider prompt，逐个生成非 user-note block，返回 `metadata.generator=selected_llm_provider`、`profile_id/model_id/model/provider/usage/finish_reason`，并把 chapter/block anchors 回填到 `source_anchors`；默认 `deterministic-agent-loop` 仍保留现有静态 fallback，避免无 provider 配置时破坏前端可用性。合同 `book_compile_page_calls_selected_provider_and_preserves_source_anchors_like_python` 覆盖 provider 请求、metadata 和 source anchors。
- Book `regenerate-block` 现在同样在 settings catalog 选中真实 OpenAI-compatible LLM 时让普通非 overview block 走 selected provider：浅合并并持久化 `params_override` 后生成 block，保留/回填 source anchors，写入 selected provider metadata、usage 和 `regenerated=true`；provider 失败不会变成 HTTP 5xx，也不会擦掉旧 payload/source_anchors/custom metadata，而是按 Python generator 失败语义返回 `status=error` 的 block、合并 `metadata.failure` 并重新聚合 page status；`concept_graph` 与无 provider 配置继续使用静态 generator，保留现有 Python-like 错误和 fallback 语义。Book WS 还补齐 Python 的 missing target 差异：找不到 regenerate 目标时返回 `regenerate_block_result { "block": null }`，而 HTTP 仍返回 404。合同 `book_regenerate_block_calls_selected_provider_and_preserves_source_anchors_like_python`、`book_regenerate_provider_failure_preserves_existing_block_state_like_python` 与 `book_ws_regenerate_missing_block_returns_null_result_like_python` 覆盖这些路径，`cargo test --test api_contract book_` 的 28 条 Book 契约通过。
- 这次只补了 active page claim、最终回答 SSE 合同、Book 普通页 selected-provider compile 优先路径，以及 Book regenerate-block selected-provider/WS missing-null/provider-failure 路径；Book 仍未完成 Python `BookCompiler` 的 LLM-first planner、SourceExplorer/RAG query 设计与 summary、各 block 类型专属 generator 质量、后台 auto_compile queue、block concurrency 和完整 source grounding 迁移。

当前剩余高风险差距：

- 新一轮只读 route/语义审计发现，轻量 parity scan 的 “Python missing 0” 不能作为完全替代证明；auth env fallback、legacy users/secret migration、`GET /api/v1/settings` 角色感知字段裁剪、settings catalog/apply admin guard 和 secret redaction 已补定向合同；PocketBase auth 的 login/status/register/admin-create/protected HTTP token refresh 已补，PB-backed completed chat turn 的 `sessions/messages/turns/turn_events` 四集合写入、detail/list 聚合、rename、cascade delete 和 `messages.metadata_json` 拒绝时的半写清理已补；PB-backed WebSocket running-turn partial persistence、subscribe/replay、terminal upsert 去重、stored running turn cancel terminal replay、regenerate 复用 PB session/user message 并替换尾部 assistant、PB-only session quiz-results 写入 PB message + question notebook 已补定向合同；但真实 PocketBase schema/migration 资产和真实 PB 实例端到端验证仍未完整覆盖。WebSocket token gate 已有定向合同测试覆盖，但仍需在真实前端/反代替换前做端到端回放
- Python 旧协议 `WS /api/v1/chat` 和 `WS /api/v1/solve` 已补 wire adapter 和关键事件 contract，`/chat` 的 `enable_web_search` 状态与 `sources.web` 分桶也已补齐，`/solve` 已补 Python 风格 `solve_...` task id、默认 `DeepSolveCapability` tools、默认 `ai-textbook` RAG 课程、provider usage -> `token_stats` 映射、Python 风格 solve 输出目录、raw `final_answer.md`、Python 形状 `scratchpad.json`、`cost_report.json`、result `output_dir/output_dir_name`、基础 metadata/cost summary 和 artifact URL rewrite；`web_search` 已有 SearXNG、Brave、Tavily、Jina 和 Serper provider-backed 执行路径，但旧协议整体仍属于 compatibility 层：还没有完整复制 Python `ChatAgent` / `MainSolver` 的 LLM streaming、DuckDuckGo/Perplexity 全 provider 检索、完整 tool execution、process log 细节、真实 ReAct scratchpad 语义和错误映射
- Knowledge task stream 已补 Python 风格 task id、SSE event payload、unknown-task 空流、500 条共享 backlog、200 条 subscriber queue、live event 推送和 `failed` SSE payload/终止语义；create/upload/reindex/sync-folder 已有 spawned worker，HTTP 会先返回 `task_id`，慢 embedding 不再阻塞响应，成功/失败终态通过 task stream 表达；reindex 成功路径已补 `processing_documents` / `Embedding batches:` 中间进度 replay；linked-folder sync 已补 `needs_reindex` 前置拒绝、缺 active index 后台失败无副作用、`last_sync/synced_files` change detection/no-op 语义和 synced 文件 active index/RAG 可见性；但 knowledge worker 还没有完整移植 Python 所有 worker 的逐文件/逐批进度细节、真实 Python traceback 精度和所有 worker 失败路径的精细语义
- TutorBot 仍缺 Python 行为面：`/btw <instruction>` 已补即时 accepted、后台 selected provider 执行、当前 WebSocket `proactive` notify，以及 active `/stop` 取消 `/btw` 并抑制取消后通知；但还没有完整复制 Python subagent 的 15 轮 tool loop、base tools、失败后由主 agent 二次自然总结、session subagents 全量取消和 system-channel session 持久化细节；`/team <goal>`/`/teams <goal>`、`status`、`log`、`stop`、`approve|reject|manual`、自然语言 approval reply 和 selected provider 初始计划已有可持久化 runtime skeleton 与 Web-visible metadata/history parity，但真实异步 workers、自动 approval prompt、team lead proactive updates 仍未完整移植；heartbeat/proactive notify queue、外部 channel runtime 仍未完整移植；MCP 已补 streamable HTTP 动态 `tools/list` schema 注册与 `tools/call` 执行回灌，但 stdio/SSE transport、完整 session protocol、schema cast/validation 和 registry 错误 hint 仍缺；`/help`、`/new`、idle `/stop`、`/restart`、usage `/btw`/`/team`、带参数 `/btw` accepted/proactive notify、team 基础 runtime 命令和 Python JSONL metadata header 已补；`/new` 已补清空前 raw archive 到共享 `SUMMARY.md` 的核心保护，但还没有完整移植 Python LLM memory consolidator、token-threshold consolidation 和 `PROFILE.md` 长期记忆更新质量
- 前端扫描里的 `/api/v1/book{param}` 是 `web/lib/book-api.ts` 中 `BASE + path` 包装导致的模板扫描伪影；真实运行路径是 `/api/v1/book/books`、`/api/v1/book/books/{book_id}` 等，Rust 已覆盖这些 Book 路由
- `/api/v1/vision/analyze` 和 `/api/v1/vision/solve` 的路由、校验、REST image URL 下载、真实 WS 握手、metadata-only 降级事件序列，以及 OpenAI-compatible 多模态 LLM 四阶段分析路径已补齐；但 Rust Vision prompts/parser 仍是轻量兼容实现，没有完整复制 Python VisionSolverAgent 的提示词、流式 chunk 处理、GeoGebra 校验/修复细节和 provider retry/error mapping
- parity scan 已无 Python-only 路由缺口的旧结论只代表扫描器未发现静态路径差异，不代表所有端点都完成完整业务语义迁移；课程 RAG 已补 selected-KB no-fallback、本地 index-version 状态合同、embedding 持久化、query/vector cosine retrieval、progress WebSocket 和 task stream 的非伪造 unknown-task/backlog/live subscriber/failed SSE runtime 语义；仍未做到完整后台 task worker、LlamaIndex node/docstore JSON schema、精确 token chunking、legacy index 迁移全兼容；unified WS 已补 replay/live subscribe/cancel/regenerate 的协议级语义，并具备 selected OpenAI-compatible LLM non-streaming 成功路径、GPT-5/reasoning Responses API 非流式成功路径、system/settings raw LLM probe 的 Responses 分支、text/base64 附件文本注入、Book context 注入、chat completion/Responses usage 和 reasoning metadata 解析、tool-call 后最终回答 provider SSE chunk 合同、provider failure terminal failed 语义，但仍未完整移植 Python ChatOrchestrator 的 streaming 阶段、Responses stream、provider retry/error mapping、图片/多模态附件 provider-specific 注入、notebook/history/question-bank/memory/skills context builder 全量消息构造；Book WS 已覆盖前端可见生成动作的协议级事件/result shape，Book overview/front-end renderer payload、非 overview Python-like block planning、ready/no-force 幂等、page status/error 聚合、deep-dive parent linkage、supplement remediation、health drift/log health、同页 active compile claim、普通页 selected-provider compile 优先路径，以及普通 block selected-provider regenerate、provider-failure inline block error 与 WS missing-null 语义；但真实 LLM BookEngine/BookCompiler 的动态规划、SourceExplorer/RAG query 设计、各 block 专属 generator 质量、后台 auto_compile worker 和完整 provider/RAG source grounding 仍需要继续做真实回放和语义 contract test；TutorBot 已接入 selected provider + RAG tool loop + canonical history，并补齐常用 slash command 的非普通聊天入口语义和 `/team <goal>/status/log/stop` 持久化 runtime skeleton，MCP streamable HTTP 已能动态注册和执行工具，但还未完整复制 Python heartbeat/proactive 通知、外部 channel runtime、真实后台 subagent、真实 team workers/approval、stdio/SSE MCP transport 和完整 MCP session 协议；Co-Writer 普通 edit 的 history/tool-call 审计语义已补齐，但真实 LLM 编辑质量、真实 RAG/Web context 注入、ReAct 流式工具执行仍是 compatibility 级；Playground plugins、legacy question WS、`deep_question` follow-up/answer-now runtime envelope 已补协议级 compatibility，但 question 真实 LLM 题目生成质量、MinerU/PDF 解析、部分失败语义、真实 FollowupAgent/answer_now prompt 质量仍需要继续补真实执行面和 contract test

## 已知限制

这是轻量审计，不是完整语义分析器，当前限制包括：

- Rust 只扫描 `backend/src/lib.rs` 里的 `.route("...", get/post/put/patch/delete(...))`，并有限识别 `get(...).post(...)` 这类 axum 链式 method router
- Python 只合成 `main.py` 里 `include_router(...)` 能看见的 prefix，并提取 `@router.get/post/put/delete/patch/websocket(...)`
- 前端只扫描 `web/app`、`web/components`、`web/lib` 下常见 TS/JS 文件
- 前端对模板字符串只做有限展开：会解析简单常量，复杂表达式会保留成 `{expr}`
- 前端 `apiUrl(...)` 本身不一定代表真实发起了请求；只有 `fetch(apiUrl(...))` 会尽量推断 HTTP method
- 不校验 query 参数、请求体、响应体、状态码、认证要求、流式协议或业务语义
- 不跟踪 Next rewrite、代理规则或运行时注入的动态 URL 构造

如果后续要做“真正的 parity 验证”，还需要再补：

- 请求/响应 schema 对比
- 认证与错误语义对比
- 流式接口与 WebSocket 事件格式对比
- 逐路径的真实回放或 contract test
